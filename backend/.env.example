# backend/.env.example

# MongoDB connection string.
# IMPORTANT: database name is "cohost2"
# Example:
# mongodb+srv://cohost_user:YourPassword@yourcluster.mongodb.net/cohost2?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster-host>/cohost2?retryWrites=true&w=majority

# JWT secret used to sign auth tokens. Use a long random string in real deployments.
JWT_SECRET=change-this-to-a-long-random-string

# OpenAI API key for analytics insights.
OPENAI_API_KEY=sk-...

# Port for local dev. Render will override this automatically with its own PORT.
PORT=3000